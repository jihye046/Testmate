<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.my.ex.UserMapper">
	<select id="getUserPassword" resultType="String">
		SELECT userPw
		  FROM exam_user
		 WHERE userId = #{userId}
	</select>
	
	<insert id="signup">
		INSERT INTO exam_user
			(
			userId,
			userPw,
			uemail
			)
		VALUES
			(
			#{userId},
			#{userPw},
			#{uemail}
			)
	</insert>
	
	<select id="checkIdDuplicate" resultType="Integer">
		SELECT COUNT(*)
		  FROM exam_user
		 WHERE userId = #{checkId}
	</select>
</mapper>